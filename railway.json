{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm run build -w @lifeos/shared && npm run build -w @serviceos/shared && npm run db:generate && npm run build -w @serviceos/api"
  },
  "deploy": {
    "startCommand": "npm run start:prod -w @serviceos/api",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
